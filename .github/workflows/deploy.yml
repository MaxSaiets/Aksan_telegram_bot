name: Deploy Bot

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy on server
        shell: powershell
        run: |
          Set-Location "C:\bots\telegram_aksan_bot"
          .\deploy.ps1
